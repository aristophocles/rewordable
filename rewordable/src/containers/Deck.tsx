import React from 'react';
import { Card } from '../types';

interface DeckProps {
    deckCards: Card[];
};

function Deck(props: DeckProps) {
    return (<button>Deck</button>);
}

export default Deck;
